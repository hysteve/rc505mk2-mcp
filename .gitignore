# Dependencies & build
node_modules/
dist/
tsconfig.tsbuildinfo

# Environment
.env
.env.*
*.local

# OS
.DS_Store
**/*/.DS_Store

# Test / generated artifacts
test-output/
generated/
rc505-backups/

# Local IDE & agent config — never commit to main
# .agents/ and .cursor/ are for repo dev/coding skills (how to work in this codebase).
# .claude/ and .vscode/ are local editor settings.
.agents
.claude
.cursor
.vscode

# MCP bundle build output (source lives in mcpb/ + npm run build)
releases/
.mcpb-staging/

# Optional local skill source (sync:skills → skills/rc505mk2/)
docs/SKILL.md
skills-lock.json

# Internal docs (not public)
docs/archive/
docs/manual-test/

# Legacy / unused
.next/
out/

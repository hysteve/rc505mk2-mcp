# Dependencies & build
node_modules/
dist/
tsconfig.tsbuildinfo

# Environment
.env
.env.*
*.local

# OS
.DS_Store
**/*/.DS_Store

# Test / generated artifacts
test-output/
generated/
rc505-backups/

# Local IDE & agent config — never commit to main
# .agents/ and .cursor/ are for repo dev/coding skills (how to work in this codebase).
# .claude/ and .vscode/ are local editor settings.
.agents
.claude
.cursor
.vscode

# RC-505 distribution packaging (local release builds — attach .mcpb via GitHub Releases)
plugin/
skills/
skills-lock.json
scripts/pack-plugin.ts
scripts/sync-skills.ts
scripts/create-icon.ts
releases/

# Internal docs (not public)
docs/archive/
docs/manual-test/
docs/SKILL.md

# Legacy / unused
.next/
out/
